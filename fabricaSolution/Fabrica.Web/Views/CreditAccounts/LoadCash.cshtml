@using Fabrica.Web.Models
@model LoadCashViewModel

@{
    ViewData["Title"] = "LoadCash";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<header class="masthead bg-primary text-white text-center">
    <div class="row" style="margin-right: 0px">
        <div class="column">
            <div class="col w-25 align-items-md-center">
                <h1 class="masthead-subheading text-uppercase text-md-right text-secondary">Load</h1>
                <h1 class="masthead-heading text-uppercase text-md-right text-secondary">Cash</h1>
            </div>
        </div>

        <div class="column">
            <div class="container w-50 align-items-md-baseline flex-column">
                <form class="mx-auto half-width" method="post" asp-antiforgery="true">
                    <div class="form-group">
                        <label asp-for="@Model.Cash"></label>
                        <input type="number" min="0.1" step="0.1" class="form-control" asp-for="@Model.Cash" placeholder="Cash amount...">
                        <span asp-validation-for="@Model.Cash" class="text-danger"></span>
                    </div>
                    <div class="button-holder d-flex justify-content-center">
                        <button type="submit" class="btn btn-outline-light">Load</button>
                    </div>
                    <br/>
                </form>

                <!-- Icon Divider -->
                <div class="divider-custom divider-light">
                    <div class="divider-custom-line"></div>
                    <div class="divider-custom-icon">
                        <i class="fas fa-star"></i>
                    </div>
                    <div class="divider-custom-line"></div>
                </div>
            </div>
        </div>
    </div>
</header>

